---
import type { Props } from "@astrojs/starlight/props";
import Default from "@astrojs/starlight/components/TwoColumnContent.astro";

import Hero from "./Hero.astro";
---

{
	Astro.props.slug === "" ? (
		<>
			<Hero />
		</>
	) : Astro.props.slug === "demo" ? (
		<slot />
	) : (
		<Default {...Astro.props}>
			<slot />
		</Default>
	)
}
