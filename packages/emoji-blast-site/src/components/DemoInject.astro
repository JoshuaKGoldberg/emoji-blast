---
import { Code } from "@astrojs/starlight/components";

type ShapeWeWant = typeof import("./examples/basic/inverse-gravity"); // too

interface Props {
	example: ShapeWeWant;
}

// const examples = import.meta.glob<
// 	typeof import("./examples/basic/inverse-gravity")
// >("./examples/*/*.ts");

// const example = await examples[`./examples/${Astro.props.importSource}`]();

const { example } = Astro.props;
console.log({ example });
---

<h1>{example.name}</h1>
<!-- <p>{example.blurb}</p> --><!-- <Code code={example.codeSnippet} lang="js" /> -->
<script>
	// // import { emojiBlast } from "emoji-blast";
	// const { explosionFunction } = await import(
	// 	`./examples/${Astro.props.importSource}`
	// );

	// emojiBlast();
	// explosionFunction();
</script>
