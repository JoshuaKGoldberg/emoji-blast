jobs:
  lint_spelling:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/prepare
      - run: pnpm lint:spelling

name: Lint spelling

on:
  pull_request: ~
  push:
    branches:
      - main
